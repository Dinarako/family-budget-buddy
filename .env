# In development, Vite proxies /api/* to the backend — leave VITE_API_URL empty.
# In production, set this to the full backend URL (e.g. https://api.yourdomain.com).
VITE_API_URL=

# Backend server settings
PORT=3001
JWT_SECRET=local-dev-secret-change-before-deploying
