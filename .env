# Frontend — URL of the local API server
VITE_API_URL=http://localhost:3001

# Backend — change JWT_SECRET to any long random string in production
PORT=3001
JWT_SECRET=local-dev-secret-change-before-deploying
